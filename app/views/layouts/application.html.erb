<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shring-to-fit=no">

    <title><%= @page_title %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="keywords" content="<%= @seo_keywords %>" />

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="site-wrapper">
      <div class="site-wrapper-inner">
        <div clas="cover-container">
          <%= render "shared/application_nav" %> <!--Partial HTML template-->

          <%= yield %>

          <%= render "shared/application_footer" %> <!--Partial HTML template-->

          <%# bikin session yang bisa dipindah2in tapi ga aman%>

          <%= source_helper("application") %>
        </div>
      </div>
    </div>
  </body>
</html>
